using UnityEngine;

public class Card : MonoBehaviour
{
    public CardData cardInfo;

    public void Start()
    {

       // Debug.Log(cardInfo.cardName + ", " + cardInfo.value + ", " + cardInfo.hpCost + ", " + cardInfo.hasSpecial);
    }
}
